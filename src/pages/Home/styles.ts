import styled from "styled-components";

export const HomeContainer = styled.main`
	.wrapper {
		max-width: 54rem;
		margin: 0 auto;
		padding: 0 1.5rem;
	}

	.home-components{
		margin-top: calc(0px - 100px);
	}
`
